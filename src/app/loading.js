import React from 'react';

const Loading = () => {
    return (
        <div className='h-[100vh] text-white bg-black flex justify-center items-center'>
     <h1>loading...</h1>       
        </div>
    );
};

export default Loading;